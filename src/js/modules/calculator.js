export const calculator = {
  "sum": (a, b) => a + b,
  "subt": (a, b) => a - b,
  "div": (a, b) => a / b,
  "mult": (a, b) => a * b,
}

export const outside = () => {
  console.log("Переменная которую не видно c наружи")
}
